# Human input required now

The five Wikimedia clips are **pilot/control** footage. They are not
hidden-state gold. Do not replace `HUMAN_FILL_REQUIRED` on those rows with
hidden-state answers.

1. Open `reports/hidden_state_candidates/index.html`.
2. Obtain **5** new clips that match `docs/HIDDEN_STATE_VIDEO_SOURCING_GUIDE.md` (opaque concealment; crop before any reveal; license recorded).
3. For each new file, keep provenance and inspect frames with `docs/HIDDEN_STATE_ELIGIBILITY.md` **before** writing `ground_truth` into a research manifest.

Self-filming is the expected path if Commons/journal search stays empty.
