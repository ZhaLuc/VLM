# Zero-shot baseline: untouched Qwen2.5-VL-7B-Instruct on held_out.
# Does not download weights unless allow_model_download is enabled intentionally.
name: baseline_qwen25vl_7b
stage: baseline
training_method: none
reward_function: exact_match
output_dir: runs
preserve_raw_outputs: true
baseline_immutable: true
allow_held_out_in_training: false
allow_model_download: false
seed: 0
notes: >
  Immutable zero-shot reference on the fixed held_out split.
  Requires a local checkpoint or an intentional download.

model:
  model_id: Qwen/Qwen2.5-VL-7B-Instruct
  revision: null
  torch_dtype: bfloat16
  device_map: auto
  trust_remote_code: true

checkpoint:
  kind: base
  path: null
  parent_run_id: null
  label: untouched_base

dataset:
  manifest: data/examples/toy_manifest.jsonl
  version: toy-v0
  split: held_out
  task: hidden_state

generation:
  max_new_tokens: 128
  temperature: 0.0
  top_p: 1.0
  top_k: 0
  do_sample: false

device:
  preference: auto
  cuda_index: null

seed_config:
  seed: 0
  deterministic_algorithms: false
  cudnn_deterministic: false
  cudnn_benchmark: false

video:
  max_frames: 8
  sample_strategy: uniform
  temporal_shuffle: false
  shuffle_seed: 0
  resize: null
