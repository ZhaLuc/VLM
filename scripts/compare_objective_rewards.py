#!/usr/bin/env python
"""Compare hidden-state and temporal/causal objective rewards (no hybrid)."""

from magic_vlm.cli import compare_objective_main

if __name__ == "__main__":
    raise SystemExit(compare_objective_main())
